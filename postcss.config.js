export default {
    plugins: {
      // Tailwind CSS como plugin de PostCSS
      tailwindcss: {}, // Esto le dice a PostCSS que use el plugin de tailwindcss
      // Autoprefixer para añadir prefijos de vendedor
      autoprefixer: {},
    },
  };
  